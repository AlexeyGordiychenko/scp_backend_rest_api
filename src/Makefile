run-dev:
	docker compose -f docker-compose.yml --profile dev up -d

stop-dev:
	docker compose -f docker-compose.yml --profile dev down

clear-dev:
	docker compose -f docker-compose.yml --profile dev down postgres-dev --volumes